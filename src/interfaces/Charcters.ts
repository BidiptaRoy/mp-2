export interface Character {
    id: number;
    name: string;
    image: string;
    race: string;
    ki: string;
    affiliation: string;

    /* included new fields for each object including race, ki, and affilitation */ 
}